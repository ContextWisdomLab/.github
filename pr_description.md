💡 **수정 내용:**
`APPROVAL_VERIFICATION_LABELS`에 포함된 라벨들에 대한 정규표현식을 매번 런타임마다 루프 내에서 다시 컴파일하는 대신, 모듈 레벨에서 딕셔너리(`_COMPILED_LABELS`)로 한 번만 미리 컴파일하고 캐싱하여 재사용하도록 변경하였습니다.

🎯 **목적 (Why):**
`opencode_review_normalize_output.py`의 `label_section` 함수는 각 라벨을 찾기 위해 호출될 때마다 내부 헬퍼 함수 `label_matches`를 통해 반복해서 `re.escape(candidate)`을 컴파일했습니다. 정규표현식 컴파일은 비교적 무거운 연산이므로 루프 안이나 자주 호출되는 함수 안에서는 비효율을 초래합니다. 이러한 불필요한 반복 런타임 컴파일을 제거하기 위해 리팩터링했습니다.

📊 **성능 측정 결과 (Measured Improvement):**
로컬 벤치마크 테스트 결과(임시 스크립트 기반), 해당 코드를 모듈 레벨에서 미리 컴파일한 정규식을 사용하도록 최적화했을 때 약간의 성능 개선을 확인했습니다. 비록 절대적인 실행 시간 단축 효과가 거대한 규모는 아닐 수 있지만, 모듈 레벨 캐싱 패턴은 가독성과 안정성, 최적의 CPU 효율성을 달성하기 위한 Python의 모범 사례(Best Practice)입니다.
